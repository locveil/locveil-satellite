# FW-2 — esp-tflite-micro v6.0.2 compat spike (docs/design/fw_execution_layer.md E-2).
# Keeper project: becomes the wake-stack component's standing build test (FW-1).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(tflm_compat)
