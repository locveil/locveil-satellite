# waveshare-lcd146 — wall enclosure (DES-8)

Design: `../../docs/design/satellite_enclosure.md` (AGREED 2026-07-18, C-1..C-9).
Print/fit iteration: **DES-9** (HW-GATED).

- **`case.py`** — the SOURCE OF TRUTH: parametric build123d model (INFRA-2 venv).
  Regenerate everything: `~/cad/build123d-env/bin/python case.py` → `build/` (gitignored).
- **`print/`** — committed **v0 snapshots** for the 3D printer: `front-shell` +
  `back-plate` as STL (slicer input) and STEP (CAD exchange). Matte white PETG;
  face-down front shell, inner-face-up plate; no supports expected except the USB
  aperture bridge. Re-export from `case.py` after any parameter change — these files
  never get hand-edited.
- **`renders/`** — the five shaded review views (R1 front iso, R2 back iso, R3 wall
  side, R4 plate inner face, R5 exploded), regenerated by the session render script
  (trimesh + matplotlib over the STLs).

Hardware inputs are MEASURED from the hash-verified vendor STEP
(`~/cad/waveshare-lcd146/`, hashes: `docs/design/assets/des7-hardware-findings.md` §2.1).
